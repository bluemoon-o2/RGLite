# RGLite 语言设计文档

## 一、语言基础信息

### 项目说明
- **语言名称**: RGLite（轻量级编程语言）
- **类型**: 解释型、动态类型、面向对象编程语言
- **核心定位**: 
  - 面向编程入门教学，语法极简易理解
  - 支持快速脚本开发，降低编码成本
- **依赖环境**: 跨平台（Windows/macOS/Linux），需安装RGLite解释器（约5MB轻量包）

## 二、核心语法规则

### 1. 基本结构规范

#### 缩进规则
支持两种缩进方式，可自由选择且互不冲突：
- 4个空格作为1级缩进
- 1个Tab键作为1级缩进（解释器会自动将Tab转换为4个空格，避免格式歧义）

#### 语句分隔
- 单个语句占1行，无需加分号`;`
- 多行写1个语句时，无需反斜杠`\`，遵循Python语法逻辑（在括号、引号等符号内自动支持多行）

#### 注释规则
- 单行注释用`#`开头
- 多行注释用`/* */`包裹（兼容初学者的直觉认知）

```plaintext
# 缩进示例：Tab与空格均可（解释器统一处理为4空格）
if 3 > 2:
	print("Tab缩进生效")  # 1个Tab
    print("空格缩进生效")  # 4个空格

# 多行语句示例：无需反斜杠，括号/引号内自动换行
total = (10 + 20 + 30
         + 40 + 50)  # 括号内多行

fruits = [
    "apple",
    "banana",
    "orange"  # 列表内多行
]

info = """这是三引号包裹的
多行字符串，
无需反斜杠"""
```

### 2. 变量与数据类型

#### 变量声明
- 动态类型，无需指定类型，直接通过`变量名 = 值`赋值即可

#### 命名规范
- 只能以字母/下划线开头
- 可包含字母、数字、下划线
- 区分大小写（如`A`和`a`是不同变量）

#### 核心数据类型

| 类型 | 说明 | 示例 |
|------|------|------|
| 数值型 | 包含整数（int）、浮点数（float） | `5`、`3.14`、`-10` |
| 字符串 | 单引号/双引号包裹，支持三引号多行字符串 | `"hello"`、`'RGLite'`、`"""多行文本"""` |
| 布尔型 | 仅两个值：True（真）、False（假） | `3 > 2 → True`、`1 == 0 → False` |
| 列表 | 有序可修改的集合，用`[]`包裹 | `[1, "apple", True]` |
| 字典 | 键值对集合，用`{}`包裹，键唯一 | `{"name": "Tom", "age": 18}` |
| 空值 | 用`None`表示"无值"状态 | `x = None` |

### 3. 控制流语句

#### （1）条件语句：if-elif-else
- 语法格式：关键字后加条件，以冒号`:`结尾，缩进块为语句体
- 支持嵌套
- 逻辑运算符：`and`（且）、`or`（或）、`not`（非）
- 比较运算符：`==`（等于）、`!=`（不等于）、`>`/`<`/`>=`/`<=`

```plaintext
score = 85
if score >= 90:
    print("优秀")
elif score >= 70:  # 仅当前面条件不成立时执行
    print("良好")
else:
    print("需努力")
```

#### （2）循环语句：for / while
- **for循环**: 用于遍历可迭代对象（列表、字符串等），语法`for 变量 in 可迭代对象:`
- **while循环**: 基于条件循环，语法`while 条件:`
- 支持`break`（跳出循环）、`continue`（跳过当前轮次）

```plaintext
# for循环遍历列表
fruits = ["apple", "banana"]
for fruit in fruits:
    print(fruit)  # 依次输出"apple"、"banana"

# while循环计数
count = 0
while count < 3:
    print(count)  # 输出0、1、2
    count = count + 1
```

### 4. 函数与类

#### （1）函数定义
- 用`def`关键字声明，语法`def 函数名(参数):`，缩进块为函数体
- 用`return`返回值（无返回则默认返回`None`）
- 支持位置参数、默认参数（默认参数需放在参数列表末尾）

```plaintext
# 带参数的函数（参数可多行写，无需反斜杠）
def calculate(
    a,
    b=2  # 默认参数
):
    return a * b

# 调用函数
result = calculate(3)  # 结果为6
```

#### （2）类与对象
- 用`class`关键字声明类，语法`class 类名:`
- 构造方法固定为`__init__`（初始化实例属性）
- 支持单继承（语法`class 子类名(父类名):`），暂不支持多重继承（简化复杂度）

```plaintext
class Dog:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def shout(self):
        print(f"{self.name}（{self.age}岁）在汪汪叫")

# 创建对象
dog = Dog("小白", 2)
dog.shout()  # 输出"小白（2岁）在汪汪叫"
```

## 三、核心特性设计

### 1. 贴近Python习惯，降低迁移成本
- 支持Tab键缩进，与Python操作逻辑一致，避免初学者因缩进方式差异产生困惑
- 多行语句无需反斜杠，直接沿用Python中"括号/引号内自动换行"的规则，减少语法记忆负担

### 2. 极简语法与实用工具
- 移除Python中易混淆的特性（如元组、复杂切片），仅保留核心功能
- 内置`print()`、`input()`、`len()`等常用函数，列表/字典默认支持`append()`、`get()`等便捷方法

### 3. 友好错误提示
- **语法错误**: 明确指出错误行号 + 通俗解释，例如`Line 5: SyntaxError - if语句后缺少冒号`
- **运行时错误**: 给出直观原因，例如`Line 8: ValueError - 列表中无指定值"orange"`

## 四、运行模式

### 交互模式
打开RGLite解释器后直接输入代码，按回车立即执行（适合测试小段代码）

### 脚本模式
将代码写入`.rglite`后缀的文件，通过命令`rglite 文件名.rglite`执行（适合完整脚本开发）

## 五、语言优势总结

1. **学习友好**: 语法极简，贴近Python但更易上手
2. **开发高效**: 支持快速脚本开发，降低编码成本
3. **跨平台**: 轻量级解释器，支持主流操作系统
4. **错误友好**: 详细的错误提示，帮助快速定位问题
5. **迁移平滑**: 从RGLite迁移到Python几乎无成本

## 六、示例代码

```plaintext
# 简单计算器示例
def calculator(a, b, operator="+"):
    """
    简单计算器函数
    支持加减乘除运算
    """
    if operator == "+":
        return a + b
    elif operator == "-":
        return a - b
    elif operator == "*":
        return a * b
    elif operator == "/":
        if b != 0:
            return a / b
        else:
            print("错误：除数不能为0")
            return None
    else:
        print("错误：不支持的运算符")
        return None

# 使用示例
result1 = calculator(10, 5)  # 默认加法
result2 = calculator(10, 5, "*")  # 乘法
print(f"10 + 5 = {result1}")
print(f"10 * 5 = {result2}")
```

RGLite语言设计旨在为编程初学者和快速开发场景提供简单、直观的编程体验。